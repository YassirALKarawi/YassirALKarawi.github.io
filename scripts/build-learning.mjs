import { mkdir, writeFile, readFile } from "node:fs/promises";
import { resolve } from "node:path";
import { course } from "./learning-curriculum.mjs";

const root = resolve(import.meta.dirname, "..");
const out = resolve(root, "learning/digital-communications");
await mkdir(out, { recursive: true });
const esc = value => String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
const url = slug => `/learning/digital-communications/${slug}.html`;
const math = latex => `<span class="math" data-math="${esc(latex)}">${esc(latex)}</span>`;
const nav = current => course.lessons.map((l, i) => `<a href="${url(l.slug)}"${l.slug === current ? ' aria-current="page"' : ""}><span>${String(i + 1).padStart(2, "0")}</span>${esc(l.title)}</a>`).join("");
const glossary = [
  ["symbol", "الرمز", "One selected waveform representing one or more bits."],
  ["bandwidth", "عرض النطاق الترددي", "The frequency range occupied or passed by a signal."],
  ["constellation", "مخطط الكوكبة", "A geometric map of possible modulation symbols."],
  ["noise", "الضوضاء", "Random disturbance added to the desired signal."],
  ["matched filter", "المرشح المطابق", "A receiver filter designed to maximize sampled SNR."],
  ["decision boundary", "حدّ القرار", "The geometric border separating detected symbols."],
  ["intersymbol interference", "التداخل بين الرموز", "Distortion from neighbouring symbols at a decision time."],
  ["channel", "القناة", "The physical medium and impairments between transmitter and receiver."]
];
const authorName = "Yassir Al-Ameen";
const authorArabic = "ياسر الأمين";
const derivations = {
  "signals-information-fundamentals": ["From symbols to bit rate", "One symbol selects one of M alternatives, so its information capacity is k=log₂M bits.", [["Count the binary labels needed for M symbols", "k=\\log_2M"], ["Transmit Rₛ symbols every second", "R_b=kR_s"], ["Isolate the required symbol rate", "R_s=\\frac{R_b}{\\log_2M}"], ["Check the unit", "\\frac{\\mathrm{bit/s}}{\\mathrm{bit/symbol}}=\\mathrm{symbol/s}"]]],
  "sampling-reconstruction": ["Why the Nyquist rate is 2B", "Sampling multiplies x(t) by an impulse train. Multiplication in time becomes convolution in frequency.", [["Write the sampling train", "p(t)=\\sum_n\\delta(t-nT_s)"], ["Transform the product", "X_s(f)=\\frac1{T_s}\\sum_kX(f-kf_s)"], ["Each replica has width 2B", "|f|\\le B"], ["Prevent adjacent replicas from overlapping", "f_s-B>B\\;\\Rightarrow\\;f_s>2B"]]],
  "quantization-pcm": ["Uniform quantization-noise power", "For a sufficiently busy input, the rounding error is modeled as uniformly distributed between −Δ/2 and +Δ/2.", [["Write the error density", "p_e(e)=\\frac1\\Delta,\\quad |e|\\le\\Delta/2"], ["The mean error is zero by symmetry", "\\mathbb E[e]=0"], ["Integrate the squared error", "\\sigma_q^2=\\int_{-\\Delta/2}^{\\Delta/2}e^2\\frac{de}{\\Delta}"], ["Evaluate the integral", "\\sigma_q^2=\\frac{\\Delta^2}{12}"]]],
  "line-coding": ["From bit duration to spectral width", "A faster bit stream compresses every pulse in time. Fourier scaling then expands its spectrum.", [["Define the bit interval", "T_b=1/R_b"], ["Represent the pulse", "p(t)=\\operatorname{rect}(t/T_b)"], ["Transform the pulse", "P(f)=T_b\\operatorname{sinc}(fT_b)"], ["The first null occurs at", "f=1/T_b=R_b"]]],
  "pulse-shaping-isi": ["Raised-cosine bandwidth", "The Nyquist minimum one-sided bandwidth is Rₛ/2. Roll-off α adds a controlled transition band.", [["Start with the minimum edge", "B_0=\\frac{R_s}{2}"], ["Add the roll-off fraction", "B=B_0+\\alpha B_0"], ["Factor the result", "B=\\frac{(1+\\alpha)R_s}{2}"], ["Check the limits", "\\alpha=0\\Rightarrow B=R_s/2,\\quad\\alpha=1\\Rightarrow B=R_s"]]],
  "ask-modulation": ["Energy of an OOK mark", "Energy is the time integral of instantaneous power. For a 1 Ω normalization, power is s₁²(t).", [["Insert the transmitted mark", "E_1=\\int_0^{T_b}A^2\\cos^2(2\\pi f_ct)dt"], ["Use the identity", "\\cos^2x=\\frac12(1+\\cos2x)"], ["An integer number of cycles makes the cosine term integrate to zero", "\\int_0^{T_b}\\cos(4\\pi f_ct)dt=0"], ["The remaining constant term gives", "E_1=\\frac{A^2T_b}{2}"]]],
  "fsk-modulation": ["Orthogonal BFSK tone spacing", "Two tones are orthogonal when their cross-correlation over Tᵦ is zero.", [["Form the inner product", "I=\\int_0^{T_b}\\cos(2\\pi f_0t)\\cos(2\\pi f_1t)dt"], ["Apply the product-to-sum identity", "2\\cos a\\cos b=\\cos(a-b)+\\cos(a+b)"], ["Choose the difference term to complete a half-cycle multiple", "2\\pi(f_1-f_0)T_b=\\pi"], ["The minimum common coherent spacing is", "\\Delta f=\\frac1{2T_b}"]]],
  "psk-dpsk": ["Coherent BPSK error probability", "The matched-filter output is Gaussian around ±√Eᵦ. An error occurs when noise crosses the zero boundary.", [["Decision statistic under bit 1", "z=\\sqrt{E_b}+n"], ["Matched-filter noise variance", "\\sigma_n^2=N_0/2"], ["Normalize the distance to the threshold", "\\frac{\\sqrt{E_b}}{\\sqrt{N_0/2}}=\\sqrt{2E_b/N_0}"], ["Use the Gaussian upper tail", "P_b=Q\\!\\left(\\sqrt{2E_b/N_0}\\right)"]]],
  "qpsk-mpsk": ["Minimum M-PSK distance", "Adjacent PSK points and the origin form an isosceles triangle with radius √Eₛ and included angle 2π/M.", [["Apply the cosine rule", "d^2=E_s+E_s-2E_s\\cos(2\\pi/M)"], ["Use 1−cos2x=2sin²x", "d^2=4E_s\\sin^2(\\pi/M)"], ["Take the positive square root", "d_{min}=2\\sqrt{E_s}\\sin(\\pi/M)"], ["Interpret the limit", "M\\uparrow\\;\\Rightarrow\\;d_{min}\\downarrow"]]],
  "qam": ["Average energy of square M-QAM", "Square QAM uses √M equally spaced amplitude levels on each of two independent axes.", [["Axis levels are odd multiples of d", "a_i\\in\\{\\pm d,\\pm3d,\\ldots\\}"], ["Average one-axis energy", "E_I=\\frac{M-1}{3}d^2"], ["I and Q contribute equally", "E_s=E_I+E_Q=2E_I"], ["Therefore", "E_s=\\frac23(M-1)d^2"]]],
  "optimal-receivers": ["Why the matched filter maximizes SNR", "At the sampling instant, the output signal is an inner product between H(f) and S(f), while noise power depends on the energy of H(f).", [["Write the output SNR ratio", "\\mathrm{SNR}=\\frac{|\\int H(f)S(f)e^{j2\\pi fT}df|^2}{(N_0/2)\\int|H(f)|^2df}"], ["Apply Cauchy–Schwarz", "|\\langle H,G\\rangle|^2\\le\\|H\\|^2\\|G\\|^2"], ["Equality requires proportional functions", "H(f)=KS^*(f)e^{-j2\\pi fT}"], ["Transform back to time", "h(t)=Ks^*(T-t)"]]],
  "ber-link-design": ["Convert SNR to Eᵦ/N₀", "Received SNR compares power inside bandwidth B, while Eᵦ/N₀ compares energy per bit with noise density.", [["Write received SNR", "\\mathrm{SNR}=\\frac{P}{N_0B}"], ["Write bit energy", "E_b=\\frac{P}{R_b}"], ["Divide by N₀", "\\frac{E_b}{N_0}=\\frac{P}{N_0R_b}"], ["Substitute the SNR expression", "\\frac{E_b}{N_0}=\\mathrm{SNR}\\frac{B}{R_b}"]]],
  "channel-coding": ["Correction capability from minimum distance", "If two valid codewords are dmin apart, bounded decoding spheres must not overlap.", [["A radius-t sphere extends t errors from each codeword", "d_{min}\\ge2t+1"], ["Isolate t", "t\\le\\frac{d_{min}-1}{2}"], ["t must be an integer", "t=\\left\\lfloor\\frac{d_{min}-1}{2}\\right\\rfloor"], ["For dmin=3", "t=1"]]],
  "ofdm": ["Subcarrier orthogonality", "OFDM spaces tones by 1/Tᵤ so that every different pair has zero inner product over the useful symbol.", [["Take subcarriers k and m", "I_{km}=\\int_0^{T_u}e^{j2\\pi(k-m)t/T_u}dt"], ["Integrate", "I_{km}=\\left.\\frac{T_u e^{j2\\pi(k-m)t/T_u}}{j2\\pi(k-m)}\\right|_0^{T_u}"], ["For integer k−m≠0, the endpoints match", "e^{j2\\pi(k-m)}=1"], ["Therefore", "I_{km}=0,\\quad k\\ne m"]]],
  "synchronization-equalization": ["MMSE equalizer as regularized inversion", "The MMSE filter minimizes expected squared decision error instead of forcing channel inversion at any noise cost.", [["Define the objective", "J(W)=\\mathbb E|X-WY|^2"], ["Use Y=HX+N and differentiate with respect to W*", "\\partial J/\\partial W^*=0"], ["Solve the normal equation", "W(|H|^2S_x+N_0)=H^*S_x"], ["Divide through", "W_{MMSE}=\\frac{H^*}{|H|^2+N_0/S_x}"]]],
  "integrated-link-design": ["Coded symbol-rate requirement", "Every transmitted symbol carries log₂M coded bits, but only the fraction Rᶜ is information.", [["Coded bit rate", "R_{coded}=\\frac{R_{info}}{R_c}"], ["Bits carried by each M-ary symbol", "k=\\log_2M"], ["Convert coded bits to symbols", "R_s=\\frac{R_{coded}}{k}"], ["Combine", "R_s=\\frac{R_{info}}{R_c\\log_2M}"]]]
};

function iconFor(label){
  const s=label.toLowerCase();
  if(/source|destination|information|bits/.test(s)) return '<svg viewBox="0 0 48 48"><path d="M12 7h18l7 7v27H12z"/><path d="M30 7v8h8M18 23h13M18 29h13M18 35h8"/></svg>';
  if(/antenna|channel|rf link|path|carrier/.test(s)) return '<svg viewBox="0 0 48 48"><path d="M24 39V25M17 41h14M24 24a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z"/><path d="M14 12a14 14 0 0 0 0 16M34 12a14 14 0 0 1 0 16M9 7a21 21 0 0 0 0 26M39 7a21 21 0 0 1 0 26"/></svg>';
  if(/filter|correlator|pulse|equalizer/.test(s)) return '<svg viewBox="0 0 48 48"><path d="M7 10h34L28 25v12l-8 4V25z"/><path d="M14 17h20"/></svg>';
  if(/fft|ifft|spectrum/.test(s)) return '<svg viewBox="0 0 48 48"><path d="M8 39V27M15 39V17M22 39V9M29 39V21M36 39V13M43 39V30"/><path d="M6 39h38"/></svg>';
  if(/noise|impairment/.test(s)) return '<svg viewBox="0 0 48 48"><path d="M5 25h5l3-12 5 25 5-30 5 31 5-22 4 8h6"/></svg>';
  if(/decision|detector|selector|demapper|decoder/.test(s)) return '<svg viewBox="0 0 48 48"><circle cx="24" cy="24" r="17"/><circle cx="24" cy="24" r="10"/><path d="m17 24 5 5 10-12"/></svg>';
  if(/timing|sample|clock|synchron/.test(s)) return '<svg viewBox="0 0 48 48"><circle cx="24" cy="25" r="17"/><path d="M24 15v11l8 5M18 5h12"/></svg>';
  if(/power|gain|loss|margin/.test(s)) return '<svg viewBox="0 0 48 48"><path d="m27 4-15 23h11l-2 17 15-25H25z"/></svg>';
  if(/encoder|coding|interleaver|mapper|modulator|dac|quantizer/.test(s)) return '<svg viewBox="0 0 48 48"><rect x="11" y="11" width="26" height="26" rx="5"/><path d="M18 19h12M18 24h7M18 29h12M5 17h6M5 24h6M5 31h6M37 17h6M37 24h6M37 31h6"/></svg>';
  return '<svg viewBox="0 0 48 48"><circle cx="24" cy="24" r="8"/><path d="M24 5v7M24 36v7M5 24h7M36 24h7M10.5 10.5l5 5M32.5 32.5l5 5M37.5 10.5l-5 5M15.5 32.5l-5 5"/></svg>';
}

function head(title, description, canonical, type = "LearningResource") {
  const jsonLd = {"@context":"https://schema.org","@type":type,name:title,description,educationalLevel:"Undergraduate",inLanguage:["en","ar"],author:{"@type":"Person",name:authorName,alternateName:["Yassir AL-Karawi",authorArabic],url:"https://yassiralkarawi.github.io/"},url:`https://yassiralkarawi.github.io${canonical}`};
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} | ${authorName}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="https://yassiralkarawi.github.io${canonical}"><link rel="icon" href="/assets/favicon.svg" type="image/svg+xml"><link rel="stylesheet" href="/assets/styles.css?v=2026-09-14"><link rel="stylesheet" href="/learning/academy.css?v=2026-09-14b"><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css"><script type="application/ld+json">${JSON.stringify(jsonLd)}</script></head>`;
}

function lessonPage(lesson, index) {
  const prev = course.lessons[index - 1], next = course.lessons[index + 1];
  const equations = lesson.equations.map(([name, latex, note], i) => `<article class="formula-card reveal-on-scroll"><span class="formula-index">${String(i + 1).padStart(2,"0")}</span><div><h3>${esc(name)}</h3><p class="before-equation"><b>Before the equation</b> Use this relation when the problem asks about ${esc(name.toLowerCase())}. First list the known quantities, convert them to SI units, and verify the stated assumptions.</p>${math(latex)}<div class="after-equation"><b>Engineering interpretation</b><p>${esc(note)}</p></div></div></article>`).join("");
  const quickExamples = lesson.practice.slice(0,2).map(([q,a],i)=>[`Quick application ${i+1}`,q,[`Identify the requested quantity and connect it to “${lesson.equations[i][0]}”.`,`Rewrite the governing relation for the unknown, then substitute the stated values while retaining units.`,`Check the scale and physical meaning of the result: ${a}`],a]);
  const allExamples = [...lesson.examples, ...quickExamples];
  const examples = allExamples.map(([title, problem, steps, answer], i) => `<article class="worked-example reveal-on-scroll"><header><span>Worked example ${i + 1}</span><h3>${esc(title)}</h3></header><div class="example-problem"><b>Problem</b><p>${esc(problem)}</p></div><ol>${steps.map(s => `<li>${esc(s)}</li>`).join("")}</ol><div class="final-answer"><span>Verified answer</span><strong>${esc(answer)}</strong></div></article>`).join("");
  const extraPractice = [[`State the assumption behind “${lesson.equations[0][0]}”.`,lesson.equations[0][2]],["Draw the complete processing order from memory.",lesson.blocks.join(" → ")+"."],["Increase the laboratory impairment from 0.15 to 0.80. What should change?","The received observation should move farther from its ideal reference, reduce the visual decision margin, or show stronger distortion; the precise appearance depends on this lesson's model."],[`Compare “${lesson.equations[0][0]}” with “${lesson.equations[1][0]}”.`,`The first relation establishes ${lesson.equations[0][2].toLowerCase()} The second establishes ${lesson.equations[1][2].toLowerCase()}`],["Give a 60-second engineering explanation of this lesson.",`A complete answer should define ${lesson.title}, explain ${lesson.outcomes.slice(0,2).join(" and ").toLowerCase()}, state one governing equation, and finish with a practical design trade-off.`]];
  const allPractice=[...lesson.practice.slice(2),...extraPractice];
  const practice = allPractice.map(([q, a], i) => `<details><summary><span>${String(i + 1).padStart(2,"0")}</span>${esc(q)}</summary><div><b>Guided solution</b><p>${esc(a)}</p></div></details>`).join("");
  const quiz = lesson.quiz.map(([q, choices, answer, explain], i) => `<fieldset data-answer="${answer}" data-explain="${esc(explain)}"><legend><span>Q${i + 1}</span>${esc(q)}</legend>${choices.map((c, j) => `<label><input type="radio" name="q${i}" value="${j}"><span>${String.fromCharCode(65 + j)}</span>${esc(c)}</label>`).join("")}<p class="question-feedback" aria-live="polite"></p></fieldset>`).join("");
  const [derivationTitle,derivationIntro,derivationSteps]=derivations[lesson.slug];
  const derivation=derivationSteps.map(([text,latex])=>`<li><div><p>${esc(text)}</p>${math(latex)}</div></li>`).join("");
  return `${head(lesson.title, lesson.summary, url(lesson.slug))}<body class="academy lesson-page" data-visual="${lesson.visual}" data-lesson="${lesson.slug}">
  <a class="skip-link" href="#main">Skip to lesson</a>
  <header class="academy-top"><a class="academy-brand" href="/learning/"><span>YA</span><b>Signal Studio<small>${authorName}</small></b></a><div class="course-crumb">Digital Communications ${lesson.part} <span>·</span> Lecture ${String(index + 1).padStart(2,"0")}</div><button id="motion-toggle" type="button" aria-pressed="false">Pause motion</button></header>
  <div class="reading-progress"><i id="reading-progress"></i></div>
  <main id="main">
    <section class="lesson-opening"><div class="opening-copy"><p class="eyebrow">Lecture ${String(index + 1).padStart(2,"0")} · Digital Communications ${lesson.part}</p><h1>${esc(lesson.title)}</h1><p class="lead">${esc(lesson.summary)}</p><div class="lecture-author"><span>Prepared &amp; taught by</span><strong>${authorName}</strong><small>${authorArabic} · University of Diyala</small></div><div class="translation-hint"><b>عربي</b><span>Select or double-click any English term to see its Arabic meaning.</span></div><div class="outcome-pills">${lesson.outcomes.map(o => `<span>${esc(o)}</span>`).join("")}</div></div><div class="hero-instrument"><div class="instrument-head"><span>LIVE SIGNAL VIEW</span><b><i></i> running</b></div><canvas id="hero-canvas" width="900" height="540" aria-label="Animated visual model for ${esc(lesson.title)}"></canvas><div class="instrument-foot"><span id="visual-caption">Change a control to explore the model</span><output id="visual-readout">t = 0.00</output></div></div></section>
    <div class="lesson-shell"><aside class="course-drawer"><div class="drawer-title"><span>COURSE MAP</span><strong>${course.lessons.length} lectures</strong></div><nav>${nav(lesson.slug)}</nav><a class="bank-link" href="/learning/digital-communications/question-bank.html">Question bank <b>${course.lessons.length * 5} MCQs</b></a></aside>
    <article class="lesson-article">
      <section id="why" class="chapter reveal-on-scroll"><span class="chapter-no">01</span><div class="chapter-title"><p>Start with meaning</p><h2>Why this topic matters</h2></div><p class="big-copy">${esc(lesson.overview)}</p><aside class="intuition"><span>INTUITION</span><p>${esc(lesson.intuition)}</p></aside><div class="term-strip">${glossary.map(([en, ar, desc]) => `<button class="term" data-en="${esc(en)}" data-ar="${esc(ar)}" data-desc="${esc(desc)}">${esc(en)}</button>`).join("")}</div></section>
      <section id="model" class="chapter"><span class="chapter-no">02</span><div class="chapter-title"><p>Mathematical foundation</p><h2>Understand, derive, then calculate</h2></div><p>Every expression begins with its physical purpose and ends with an engineering interpretation. Read the explanation first; the equation is a compressed statement of that explanation.</p><div class="formula-grid">${equations}</div><div class="derivation-theatre reveal-on-scroll"><header><span>STEP-BY-STEP DERIVATION</span><h3>${esc(derivationTitle)}</h3><p>${esc(derivationIntro)}</p></header><ol class="derivation-steps">${derivation}</ol></div><aside class="math-note"><b>How to study the mathematics</b><p>First name every symbol, then state the assumptions, substitute values with units, and only then calculate. A numerically correct result without assumptions is not a complete engineering answer.</p></aside></section>
      <section id="system" class="chapter"><span class="chapter-no">03</span><div class="chapter-title"><p>Animated architecture</p><h2>Follow the real signal path</h2></div><div class="flow-diagram" id="flow-diagram">${lesson.blocks.map((b,i) => `<div class="flow-node"><span>${String(i+1).padStart(2,"0")}</span><div class="node-icon">${iconFor(b)}</div><strong>${esc(b)}</strong><small>processing stage</small></div>${i < lesson.blocks.length-1 ? '<i><b></b><b></b><b></b></i>' : ''}`).join("")}</div><p id="flow-caption" class="flow-caption">The luminous packet follows the information through the engineering chain.</p></section>
      <section id="lab" class="chapter lab-chapter"><span class="chapter-no">04</span><div class="chapter-title"><p>Manipulate the model</p><h2>Interactive signal laboratory</h2></div><div class="lab-grid"><div class="lab-controls"><label>Information pattern<input id="lab-bits" value="10110100" maxlength="12" inputmode="numeric"><small>binary sequence, 3–12 bits</small></label><label>Primary parameter <output id="primary-out">4</output><input id="primary" type="range" min="1" max="12" value="4"></label><label>Noise / impairment <output id="noise-out">0.15</output><input id="noise" type="range" min="0" max="100" value="15"></label><label>Display speed <output id="speed-out">1.0×</output><input id="speed" type="range" min="2" max="20" value="10"></label><button id="randomize" type="button">Generate new observation</button></div><div class="lab-screen"><canvas id="lab-canvas" width="1100" height="620" aria-label="Interactive signal analysis"></canvas><div class="screen-legend"><span><i class="cyan"></i>ideal/reference</span><span><i class="gold"></i>transmitted</span><span><i class="pink"></i>received/error</span></div></div></div><div class="observation-prompt"><b>Observe → explain</b><p id="observation-text">Move one control at a time. Describe which geometric or time-domain feature changed and why.</p></div></section>
      <section id="examples" class="chapter"><span class="chapter-no">05</span><div class="chapter-title"><p>From concepts to confident calculation</p><h2>${allExamples.length} fully worked examples</h2></div><p>Each solution identifies the governing principle, shows the substitution or reasoning, checks the result, and states its physical meaning.</p><div class="example-stack">${examples}</div></section>
      <section id="practice" class="chapter"><span class="chapter-no">06</span><div class="chapter-title"><p>Deliberate practice</p><h2>${allPractice.length} problems with guided solutions</h2></div><p>Attempt each problem on paper before opening the solution. Include units, state the selected formula, and finish with one sentence interpreting the answer.</p><div class="practice-accordion">${practice}</div></section>
      <section id="quiz" class="chapter"><span class="chapter-no">07</span><div class="chapter-title"><p>End-of-lesson assessment</p><h2>Mastery quiz</h2></div><form class="mastery-quiz" id="mastery-quiz">${quiz}<button class="grade-button" type="submit">Grade this lesson</button><div class="score-card" id="score-card" aria-live="polite"><span>Your result</span><strong>— / 5</strong><p>Answer all questions, then grade the lesson.</p></div></form></section>
      <section id="references" class="chapter reference-chapter"><span class="chapter-no">08</span><div class="chapter-title"><p>Academic foundation</p><h2>Study references</h2></div><ol><li>J. G. Proakis and M. Salehi, <em>Digital Communications</em>, McGraw-Hill.</li><li>S. Haykin, <em>Communication Systems</em>, Wiley.</li><li>B. Sklar, <em>Digital Communications: Fundamentals and Applications</em>, Pearson.</li><li>B. P. Lathi and Z. Ding, <em>Modern Digital and Analog Communication Systems</em>, Oxford University Press.</li></ol><p class="source-note">Notation and conventions can differ across textbooks. This lesson states its energy, bandwidth and noise conventions beside each formula so that comparisons remain physically meaningful.</p></section>
      <section class="lesson-close"><span>${authorName} · ${authorArabic}</span><p>Explain it without notes, solve it with units, then test it in the laboratory.</p><div>${prev ? `<a href="${url(prev.slug)}">← ${esc(prev.title)}</a>` : '<a href="/learning/">← Course overview</a>'}${next ? `<a class="next" href="${url(next.slug)}">${esc(next.title)} →</a>` : '<a class="next" href="/learning/digital-communications/question-bank.html">Final question bank →</a>'}</div></section>
    </article></div>
  </main><div class="term-popover" id="term-popover" hidden><button type="button" aria-label="Close">×</button><span>ARABIC TRANSLATION</span><strong dir="rtl"></strong><small></small><p dir="rtl"></p><a id="translate-more" target="_blank" rel="noopener">Extended translation ↗</a></div>
  <script src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js" defer></script><script src="/learning/academy.js?v=2026-09-14b" defer></script></body></html>`;
}

function indexPage() {
  const cards = course.lessons.map((l,i) => `<a class="course-card reveal-on-scroll" href="${url(l.slug)}" data-lesson-card="${l.slug}"><div class="card-top"><span>${String(i+1).padStart(2,"0")}</span><b>Digital Communications ${l.part}</b><i></i></div><h3>${esc(l.title)}</h3><p>${esc(l.summary)}</p><div class="card-foot"><span data-card-progress>Not started</span><strong>Open lecture →</strong></div></a>`).join("");
  return `${head("Digital Communications Learning Studio", course.description, "/learning/", "Course")}<body class="academy hub-page"><a class="skip-link" href="#main">Skip to course</a><header class="academy-top"><a class="academy-brand" href="/"><span>YA</span><b>Signal Studio<small>${authorName}</small></b></a><nav><a href="/">Academic home</a><a href="/publications.html">Research</a><a aria-current="page" href="/learning/">Learning</a></nav></header><main id="main">
  <section class="hub-hero"><div class="hub-copy"><p class="eyebrow">Undergraduate engineering · visual learning</p><h1>See the signal.<br><em>Understand the system.</em></h1><p>${esc(course.description)} Every lecture combines rigorous English explanation, professional mathematics, animated signal analysis, worked examples, Arabic terminology and assessed practice.</p><div class="hub-author"><span>Designed and taught by</span><strong>${authorName}</strong><small>${authorArabic}</small></div><div class="hero-actions"><a href="${url(course.lessons[0].slug)}">Start the course <span>→</span></a><a href="/learning/digital-communications/question-bank.html">Open ${course.lessons.length * 5}-question bank</a></div><div class="hero-stats"><span><b>${course.lessons.length}</b> complete lectures</span><span><b>${course.lessons.length * 4}</b> worked examples</span><span><b>${course.lessons.length * 8}</b> solved exercises</span><span><b>${course.lessons.length * 5}</b> explained MCQs</span></div></div><div class="hero-console"><div class="console-bar"><i></i><i></i><i></i><span>LIVE COMMUNICATION CHAIN</span></div><canvas id="hub-canvas" width="960" height="700" aria-label="Animated communication system"></canvas><div class="console-equation">bits → waveform → channel → evidence → decision</div></div></section>
  <section class="course-intro"><div><p class="eyebrow">Course architecture</p><h2>A complete path, not disconnected notes</h2></div><p>Part I builds representations and modulation. Part II develops optimum receivers, reliability, coding, OFDM and full link design. Progress is stored only in this browser.</p></section>
  <section class="course-ribbon"><div><span>PART I</span><b>Waveforms and modulation</b></div><i></i><div><span>PART II</span><b>Receivers and modern links</b></div></section>
  <section class="course-grid">${cards}</section>
  <section class="method-section"><div><p class="eyebrow">The learning loop</p><h2>Read → manipulate → calculate → prove</h2></div><div class="method-grid"><article><span>01</span><h3>Build intuition</h3><p>Plain English explains what changes physically before notation is introduced.</p></article><article><span>02</span><h3>Read the equation</h3><p>Every formula states assumptions, units and the design meaning of each relationship.</p></article><article><span>03</span><h3>Touch the signal</h3><p>Change values and watch waveforms, samples, spectra, constellations and decisions respond.</p></article><article><span>04</span><h3>Earn mastery</h3><p>Worked examples lead into guided problems and a scored quiz with explanatory feedback.</p></article></div></section>
  </main><footer class="academy-footer"><span>${authorName} · ${authorArabic} · University of Diyala</span><span>Digital Communications Learning Studio</span></footer><script src="/learning/academy.js?v=2026-09-14b" defer></script></body></html>`;
}

function questionBankPage() {
  const all = course.lessons.flatMap((l,li) => l.quiz.map((q,qi) => ({lesson:l, li, qi, q})));
  const groups = course.lessons.map((l,li) => `<section class="bank-group"><header><span>${String(li+1).padStart(2,"0")}</span><div><p>Digital Communications ${l.part}</p><h2>${esc(l.title)}</h2></div></header>${l.quiz.map(([q,choices,a,why],qi) => `<fieldset data-answer="${a}" data-explain="${esc(why)}"><legend>${esc(q)}</legend>${choices.map((c,j)=>`<label><input type="radio" name="b${li}-${qi}" value="${j}"><span>${String.fromCharCode(65+j)}</span>${esc(c)}</label>`).join("")}<p class="question-feedback"></p></fieldset>`).join("")}</section>`).join("");
  return `${head("Digital Communications Question Bank", `${all.length} multiple-choice questions with explanations across the complete course.`, "/learning/digital-communications/question-bank.html", "Quiz")}<body class="academy bank-page"><header class="academy-top"><a class="academy-brand" href="/learning/"><span>YA</span><b>Signal Studio<small>${authorName}</small></b></a><div class="course-crumb">Cumulative assessment</div><a href="/learning/">Course map</a></header><main id="main"><section class="bank-hero"><p class="eyebrow">Cumulative mastery · ${authorName}</p><h1>Digital Communications<br>Question Bank</h1><p>${all.length} multiple-choice questions with immediate reasoning. Use lesson mode for study or grade all visible questions for a cumulative result.</p><div><span><b>${all.length}</b> MCQs</span><span><b>${course.lessons.length}</b> topics</span><span><b>100%</b> explained</span></div></section><form id="bank-form" class="bank-shell">${groups}<div class="bank-submit"><button class="grade-button" type="submit">Grade complete bank</button><div class="score-card" id="score-card"><span>Cumulative result</span><strong>— / ${all.length}</strong><p>Answers and explanations appear after grading.</p></div></div></form></main><footer class="academy-footer"><span>${authorName} · ${authorArabic} · University of Diyala</span><a href="/learning/">Return to course</a></footer><script src="/learning/academy.js?v=2026-09-14b" defer></script></body></html>`;
}

await writeFile(resolve(root, "learning/index.html"), indexPage());
for (const [i, lesson] of course.lessons.entries()) await writeFile(resolve(out, `${lesson.slug}.html`), lessonPage(lesson, i));
await writeFile(resolve(out, "question-bank.html"), questionBankPage());

const learningUrls = ["/learning/", ...course.lessons.map(l => url(l.slug)), "/learning/digital-communications/question-bank.html"];
const sitemapPath = resolve(root, "sitemap.xml");
let sitemap = await readFile(sitemapPath, "utf8");
const existing = [...sitemap.matchAll(/<loc>https:\/\/yassiralkarawi\.github\.io([^<]*)<\/loc>/g)].map(m => m[1]).filter(path => !path.startsWith("/learning"));
const paths = [...new Set([...existing, ...learningUrls])];
const lastmod = "2026-09-14";
sitemap = `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${paths.map(path => `  <url><loc>https://yassiralkarawi.github.io${path}</loc><lastmod>${lastmod}</lastmod></url>`).join("\n")}\n</urlset>\n`;
await writeFile(sitemapPath, sitemap);
await writeFile(resolve(root, "sitemap.txt"), paths.map(path => `https://yassiralkarawi.github.io${path}`).join("\n"));
console.log(`Built ${course.lessons.length} lectures and ${allCount()} assessment questions.`);
function allCount(){ return course.lessons.reduce((n,l)=>n+l.quiz.length,0); }
